# Defer Code Mode and semantic execution

The first unified release standardizes OKF v0.2 parsing, inspection,
validation, storage adapters, lifecycle operations, and visualization. Code
Mode, DataBook semantic execution, source-archive behavior, Zotero replacement,
and cross-Bundle discovery remain separate future decisions. Adding them now
would make the base format contract depend on unrelated ownership and runtime
rules.
